const cart=[],$=s=>document.querySelector(s);function draw(){$("#count").textContent=cart.length;$("#items").innerHTML=cart.length?cart.map(x=>`<div class="cartline"><span>${x.name}</span><b>$${x.price.toFixed(2)}</b></div>`).join(""):"<p>Your cart is empty.</p>";$("#total").textContent="$"+cart.reduce((s,x)=>s+x.price,0).toFixed(2)}document.querySelectorAll(".add").forEach(b=>b.onclick=()=>{cart.push({name:b.dataset.name,price:+b.dataset.price});draw();openCart()});function openCart(){$("#cart").classList.add("open");$("#overlay").classList.add("show")}function closeCart(){$("#cart").classList.remove("open");$("#overlay").classList.remove("show")}$("#cartBtn").onclick=openCart;$("#close").onclick=closeCart;$("#overlay").onclick=closeCart;$("#year").textContent=new Date().getFullYear();document.querySelectorAll(".heart").forEach(h=>h.onclick=()=>h.textContent=h.textContent==="♡"?"♥":"♡");